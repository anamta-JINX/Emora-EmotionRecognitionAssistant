# OpenCV DNN face detector resources

Copy these files from the old project into this folder:

- `deploy.prototxt`
- `res10_300x300_ssd_iter_140000.caffemodel`

The main Flask emotion detection currently uses OpenCV's bundled Haar cascade.
These DNN files are retained for the manual-training utility and future use.
